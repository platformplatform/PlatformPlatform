{
  "name": "back-office-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run update-translations && npm run swagger && concurrently 'npm:watch:*'",
    "watch:rsbuild": "rsbuild dev",
    "watch:tsr": "tsr watch",
    "build": "npm run update-translations && npm run swagger && tsr generate && rsbuild build",
    "update-translations": "lingui extract && lingui compile --typescript",
    "check": "biome check",
    "format": "biome format",
    "swagger": "openapi-typescript lib/api/BackOffice.Api.json -o lib/api/api.generated.d.ts && biome format --write lib/api/api.generated.d.ts",
    "publish": "npm run build && mkdir -p ../Api/publish/WebApp/dist/ && rsync -r ./dist/ ../Api/publish/WebApp/dist/"
  },
  "dependencies": {
    "react-aria-components": "^1.2.1",
    "react-dom-confetti": "^0.2.0"
  },
  "devDependencies": {
    "@repo/build": "*",
    "@repo/config": "*"
  },
  "browserslist": [
    "last 1 version",
    "> 1%",
    "not dead"
  ]
}
